/**
 * Contenido institucional editorial (historia, misión, visión, sellos,
 * valores). Se centraliza aquí para reemplazarlo fácilmente cuando la
 * dirección entregue los textos oficiales — no se inventa contenido.
 * Los arreglos vacíos se muestran como "contenido en preparación" en
 * lugar de Lorem Ipsum.
 */

export const HISTORY: { paragraphs: string[] } = {
  paragraphs: [],
};

export const MISSION: string | null = null;
export const VISION: string | null = null;

export const EDUCATIONAL_SEALS: { title: string; description: string }[] = [];

export const VALUES: { title: string; description: string }[] = [];
